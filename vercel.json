{
  "version": 2,
  "builds": [
    {
      "src": "next.config.ts",
      "use": "@vercel/next"
    }
  ],
  "routes": [],
  "env": {
    "DATABASE_URL": "${env:DATABASE_URL}",
    "NEXT_PUBLIC_API_URL": "${env:NEXT_PUBLIC_API_URL}"
  },
  "outputDirectory": ".next",
  "cleanUrls": true,
  "trailingSlash": false,
  "framework": "nextjs"
}
